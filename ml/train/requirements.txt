# Training + ONNX-export dependencies for the distilled medication extractor.
# Installed into a pinned Python 3.12 virtualenv via uv (see README.md) — no
# conda assumptions. torch's CUDA build is selected automatically by uv's
# --torch-backend=auto against the local driver; CPU works too, just slower.
torch>=2.4
transformers>=4.44,<5
datasets>=2.20
seqeval>=1.2.2
numpy>=1.26
pyyaml>=6.0
matplotlib>=3.8
onnx>=1.16
onnxruntime>=1.18
optimum[onnxruntime]>=1.21
