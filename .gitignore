# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr
coverage
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo

.eslintcache

# Cypress
/cypress/videos/
/cypress/screenshots/

# Vitest
__screenshots__/

# Vite
*.timestamp-*-*.mjs

test-results/
playwright-report/

# Local Claude Code settings
.claude/settings.local.json

# Env files / local secrets (never commit API keys)
.env
.env.*
.dev.vars
.dev.vars.*

# Wrangler
.wrangler/

# Site-health audit screenshots (uploaded as workflow artifacts, never committed)
site-health-artifacts/

# ML distillation dataset — synthetic, but thousands of clinical-style notes
# don't belong in the repo; the ml/datagen scripts are the artifact. The
# exported student model (public/models/) IS committed — that's the shipped
# deliverable — but training data and intermediate outputs are not.
ml/data/
ml/train/outputs/
ml/train/.venv/
ml/train/runs/

# Python caches (training/pytest byproducts)
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Private engineering docs (public repo → these stay local only; mirror to a
# private repo/drive for backup — see docs/internal/README.md).
docs/internal/

# ToS watchdog: local clone of the private snapshot repo (never committed here)
tos-snapshots/
